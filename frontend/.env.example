REACT_APP_BACKEND_URL=http://localhost:8000
# Leave blank to use the mock Google sign-in form (no real Google Cloud project needed).
# Set to a real OAuth Web client ID (matching GOOGLE_MODE=live in backend/.env) to enable
# the real "Sign in with Google" button.
REACT_APP_GOOGLE_CLIENT_ID=
